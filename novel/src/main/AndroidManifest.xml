<manifest package="com.apache.fastandroid.novel"

          xmlns:android="http://schemas.android.com/apk/res/android"
>

    <application android:allowBackup="true"
                 android:label="@string/app_name"
                 android:supportsRtl="true"
    >
        <activity android:name=".find.detail.BookDetailActivity"/>
        <activity android:name=".bookshelf.ReadActivity"/>
    </application>

</manifest>
