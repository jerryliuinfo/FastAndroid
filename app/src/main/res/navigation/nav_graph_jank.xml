<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/messageList">

    <fragment
        android:id="@+id/messageList"
        android:name="com.apache.fastandroid.demo.performance.jankstats.MessageListFragment"
        android:label="Message List"
        tools:layout="@layout/fragment_recycleview">
        <action
            android:id="@+id/action_messageList_to_messageContent"
            app:destination="@id/messageContent" />
    </fragment>

    <fragment
        android:id="@+id/messageContent"
        android:name="com.apache.fastandroid.demo.performance.jankstats.MessageContentFragment"
        android:label="Message Content"
        tools:layout="@layout/fragment_message_content">
        <argument
            android:name="title"
            app:argType="string" />
    </fragment>

<!--    <fragment-->
<!--        android:id="@+id/composeList"-->
<!--        android:name="com.example.jankstats.compose.ComposeListFragment"-->
<!--        android:label="Compose List">-->
<!--        <action-->
<!--            android:id="@+id/action_composeList_to_messageContent"-->
<!--            app:destination="@id/messageContent" />-->
<!--    </fragment>-->
    <action
        android:id="@+id/action_global_composeList"
        app:destination="@id/composeList"
        app:launchSingleTop="true" />
</navigation>
